# Chromeppuccin
A dark catppuccin theme for chrome

## Install
```
git clone https://github.com/FaithfullyOSS/Chromeppuccin
```
In your URL bar go to `chrome://extensions` and drag the `chrome-theme` folder onto the page, enable the theme.
